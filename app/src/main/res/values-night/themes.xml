<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:backgroundTint">@color/grayStroke</item>
        <item name="colorPrimary">@color/grayStroke</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/grayStroke</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_black</item>
        <!-- Customize your theme here. -->

        <item name="android:textColor">@color/white</item>


    </style>

    <style name="tabLayout" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="tabSelectedTextColor">@color/blueFB</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/white</item>
        <item name="android:background">@color/primary_gray</item>
        <item name="tabTextColor">@color/white</item>
    </style>

    <style name="floatingActionButton" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="android:backgroundTint">@color/blueFB</item>
    </style>

</resources>